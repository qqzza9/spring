package com.joongang.persistence;

/*
import java.sql.Timestamp;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

import com.joongang.domain.ReservationVO;
import com.joongang.mapper.ReservationMapper;

import lombok.Setter;
import lombok.extern.log4j.Log4j2;

@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(classes = {com.joongang.config.RootConfig.class,
		com.joongang.config.SecurityConfig.class})
@Log4j2
public class ReservationMapperTests {
	@Setter(onMethod_ =  @Autowired) 
	private ReservationMapper mapper;
	
	//@Test
	public void testInsertReservation() {
		ReservationVO vo = new ReservationVO();
		vo.setUserid("hong");
		vo.setSno(1);
		vo.setTno(2);
		vo.setResdate(Timestamp.valueOf("2023-06-30 00:00:00"));
				
		mapper.insert(vo);	
	}
}
*/